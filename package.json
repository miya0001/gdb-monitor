{
  "name": "geonicdb-pulse",
  "version": "1.0.0",
  "private": true,
  "description": "GeonicDB Pulse — realtime entity monitor with Geolonia Maps",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.1",
    "vite-plugin-minify": "^2.1.0"
  },
  "dependencies": {
    "@geolonia/geonicdb-sdk": "^0.1.0"
  }
}
